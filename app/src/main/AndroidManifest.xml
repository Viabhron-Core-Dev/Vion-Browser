<?xml version="1.0" encoding="utf-8"?>

<!--
  Copyright 2015-2016 Soren Stoutner <soren@stoutner.com>.
  Copyright 2026 Vion Browser Contributors.

  This file is part of Vion Browser.

  Vion Browser is free software: you can redistribute it and/or modify
  it under the terms of the GNU General Public License as published by
  the Free Software Foundation, either version 3 of the License, or
  (at your option) any later version.

  Vion Browser is distributed in the hope that it will be useful,
  but WITHOUT ANY WARRANTY; without even the implied warranty of
  MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
  GNU General Public License for more details.

  You should have received a copy of the GNU General Public License
  along with Vion Browser.  If not, see <http://www.gnu.org/licenses/>. -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vion.browser">

    <!-- Required to load websites. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to create homescreen shortcuts. -->
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <!-- Required for WindowManager overlay (AI window). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/VionBrowser"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <!--
          android:configChanges="orientation|screenSize" prevents reload on rotation, preserving WebView scroll.
          android:launchMode="singleTask" reuses existing activity instead of launching a new one.
          android:persistableMode="persistNever" removes app from recents on reboot.
        -->
        <activity
            android:name=".MainWebViewActivity"
            android:label="@string/app_name"
            android:theme="@style/VionBrowser.MainWebView"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTask"
            android:persistableMode="persistNever"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Handle web intents so links open in Vion Browser. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings"
            android:theme="@style/VionBrowser.Settings"
            android:parentActivityName=".MainWebViewActivity"
            android:configChanges="orientation|screenSize"
            android:persistableMode="persistNever" />

        <activity
            android:name=".AboutActivity"
            android:label="@string/about_vion"
            android:theme="@style/VionBrowser.About"
            android:parentActivityName=".MainWebViewActivity"
            android:configChanges="orientation|screenSize"
            android:persistableMode="persistNever" />

    </application>

</manifest>
