<?xml version="1.0" encoding="utf-8"?>
<!--
  Copyright 2026 Vion Browser Contributors. GPL v3.
  Phase 6 — Unified Panel (Bookmarks / History / Saved / Downloads)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/vion_primary">

    <!-- Tab strip -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/panel_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@color/toolbar_background"
        app:tabTextColor="@color/text_secondary"
        app:tabSelectedTextColor="@color/vion_accent"
        app:tabIndicatorColor="@color/vion_accent"
        app:tabMode="fixed"
        app:tabGravity="fill" />

    <!-- Content pager -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/panel_view_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
